#!/bin/bash

#java -cp "$(cat /tmp/cp.txt):experiments" CNNMnistExample

#mvn dependency:build-classpath -Dmdep.outputFile=/tmp/cp.txt -q && \
#  java -cp "$(cat /tmp/cp.txt):target/classes:experiments" ProcessMnistExample

# Or if you've already built the classpath file recently:

#java -cp "$(cat /tmp/cp.txt):target/classes:." ProcessMnistExample

cd ..
javac -cp "$(cat /tmp/cp.txt):target/classes" -d experiments experiments/ProcessMnistHybridExample.java
cd experiments

java -cp "$(cat /tmp/cp.txt):target/classes:." ProcessMnistHybridExample

